import static javax.swing.JOptionPane.*;
public class övnig1 {
	public static void main(static[] arg) {
		int antalDagar = 0;
		int tal = 0;
		int summa = 0;
		String indataMånad = showInputDialog("antal Månad");
		antalDagar = Integer.Parelnt(indata);
		while(indataMånad < 12) {
			
			int total = integer.parselnt(indata);
			indataGigabyte = showInputDialog ("hur många GB använde du?");
			
			if(total < 100) {
				total = 100-total;
				summa = total+ summa;
			} else if (tal == antalDagar) {
				break;
				JOptionPane.showmessegeDialog (null, summa)
			}
		}
	}
			
			